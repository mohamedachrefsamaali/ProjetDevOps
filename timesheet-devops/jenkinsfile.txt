pipeline {
    agent any

    stages {
        
        stage('Compile') {
            steps {
                script {
                    sh 'mvn clean compile'
                }
            }
        }
    }
}
